plugins {
    id 'java-conventions'
    id 'java-library'
}

group 'com.arthurlamberti.customerwallet.application'

repositories {
    mavenCentral()
}

dependencies {
    implementation(project(':domain'))
    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}